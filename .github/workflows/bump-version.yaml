name: Version bump

on: push

jobs:
  bump-version-major:
    name: Bump major version
    if: contains(github.event.comment.body, 'bump@major')
    runs-on: ubuntu-latest

    steps:
      - name: Update major
        run: |
          echo major

#  bump-version-minor:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Update minor
#        if: contains(github.event.comment.body, 'bump@minor')
#        run: |
#          echo "minor"
#
#  bump-version-patch:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Update patch
#        if: contains(github.event.comment.body, 'bump@patch')
#        run: |
#          echo "patch"
