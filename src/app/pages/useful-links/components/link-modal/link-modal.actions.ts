import { createAction } from '@ngrx/store';

export const ModalContinue = createAction('[LinkModalPage] Continue on Modal');

export const ModalCancel = createAction('[LinkModalPage] Cancel on Modal');
