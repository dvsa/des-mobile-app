import { createAction } from '@ngrx/store';

export const FutureTestModalCancelButton = createAction(
  '[DelegatedRekeySearch] Future Test Modal - Cancel Button Clicked'
);

export const FutureTestModalContinueButton = createAction(
  '[DelegatedRekeySearch] Future Test Modal - Continue Button Clicked'
);
